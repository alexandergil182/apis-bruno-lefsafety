info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{api_url_local}}/v1/api/auth/login/"
  body:
    type: json
    data: '{"email": "epalau@gmail.com", "password": "luciana2017"}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
