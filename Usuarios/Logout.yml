info:
  name: Logout
  type: http
  seq: 3

http:
  method: POST
  url: "{{api_url_local}}/v1/api/auth/logout/"
  body:
    type: json
    data: "{\"refresh\": \"eyJ...\"}"
  auth:
    type: bearer
    bearer:
      token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
