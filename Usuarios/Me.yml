info:
  name: Me
  type: http
  seq: 4

http:
  method: GET
  url: "{{api_url_local}}/v1/api/auth/me/"
  body:
    type: none
  auth:
    type: bearer
    bearer:
      token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
