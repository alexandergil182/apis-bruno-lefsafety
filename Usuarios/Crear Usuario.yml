info:
  name: Crear Usuario
  type: http
  seq: 8

http:
  method: POST
  url: "{{api_url_local}}/v1/api/auth/usuarios/"
  body:
    type: json
    data: |-
      {
        
       "email": "a@lexsafety.com", 
       "nombre": "Alexander", 
       "apellido": "Gil Tafur", 
       "rol": "ABOGADO", 
       "password": "luciana2017", 
       "password_confirm": "luciana2017"

      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
