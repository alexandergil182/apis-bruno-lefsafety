info:
  name: Cambiar Password
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url_local}}/v1/api/auth/me/cambiar-password/"
  body:
    type: json
    data: "{\"password_actual\": \"mipassword123\", \"password_nuevo\": \"nuevopassword456\", \"password_nuevo_confirm\": \"nuevopassword456\"}"
  auth:
    type: bearer
    bearer:
      token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
