info:
  name: Refresh Token
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url_local}}/v1/api/auth/token/refresh/"
  body:
    type: json
    data: "{\"refresh\": \"eyJ...\"}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
