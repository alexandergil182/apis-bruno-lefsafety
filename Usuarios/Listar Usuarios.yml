info:
  name: Listar Usuarios
  type: http
  seq: 7

http:
  method: GET
  url: "{{api_url_local}}/v1/api/auth/usuarios/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
