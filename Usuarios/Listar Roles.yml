info:
  name: Listar Roles
  type: http
  seq: 11

http:
  method: GET
  url: "{{api_url_local}}/v1/api/auth/roles/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
