info:
  name: Detalle Conjunto
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url_local}}/v1/api/conjuntos/1/"
  body:
    type: none
  auth:
    type: bearer
    bearer:
      token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
